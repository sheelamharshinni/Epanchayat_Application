<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="NormalText" parent="AppTheme">
        <item name="fontFamily">@string/Roboto_Regular_font</item>
        <item name="fontFamilyType">@string/Roboto_Regular</item>
    </style>

    <style name="HeaderStyle" parent="@android:style/Widget.TextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/colorTitle</item>
        <item name="android:textSize">@dimen/txt_large_size</item>
        <item name="fontFamily">@string/Roboto_Bold_font</item>
        <item name="fontFamilyType">@string/Roboto_Bold</item>
    </style>

    <style name="NormalHeaderStyle" parent="@android:style/Widget.TextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/colorTitle</item>
        <item name="android:textSize">@dimen/txt_large_size</item>
        <item name="fontFamily">@string/Roboto_Regular_font</item>
        <item name="fontFamilyType">@string/Roboto_Regular</item>
    </style>

    <style name="SubHeaderStyle" parent="@android:style/Widget.TextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/colorSubtitle</item>
        <item name="android:textSize">@dimen/txt_small_size</item>
        <item name="fontFamily">@string/Roboto_Regular_font</item>
        <item name="fontFamilyType">@string/Roboto_Regular</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
</resources>
